export interface Testimonial {
  name: string;
  title: string;
  message: string;
}

export interface PricingPlan {
  name: string;
  price: number;
  badge: string;
  description: string;
  features: string[];
}
